import SwiftUI

// Legacy theme names kept for backward compatibility, forwarding to asset-backed colors.
extension Color {
    static let highlightTerracotta: Color = .accentPrimary
    static let highlightGolden: Color = .surfacePrimary
    static let highlightCream: Color = .backgroundPrimary
    static let highlightDarkBackground: Color = .backgroundDarkPrimary
    static let highlightEspresso: Color = .textPrimary
    static let highlightMuted: Color = .containerPrimary
}
